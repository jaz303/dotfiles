# Disable host key checking for DHCP hosts on local network
# Useful for Raspberry Pi's etc. with frequently changing
# OS
Host 172.20.250.*
    StrictHostKeyChecking no
    UserKnownHostsFile=/dev/null

# Load local (non-git) config from ~/.ssh/config.d
Include config.d/*

